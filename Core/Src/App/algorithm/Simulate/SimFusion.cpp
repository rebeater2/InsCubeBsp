/**
* @file SimFusion.cpp in InsCubeBsp
* @author linfe
* @comment
* Create on 2022/11/20 12:01
* @version 1.0
**/


#include "SimFusion.h"
#include "FileRW.h"


/*
int main(int argc, char** argv){
    if (argc < 2){
        return 1;
    }
    LogReader reader(argv[1]);
    DataType::recorder_data_t recorder_data;
    while (reader.isOk()){
        reader.ReadNext(recorder_data);
        if (recorder_data.type == DataType::RECORDER_TYPE_IMU){

        }
        if (recorder_data.type == DataType::RECORDER_TYPE_GNSS){
        }

    }

}*/
